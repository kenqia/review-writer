# Independent V3.1.1 Exact-Claim Layer B Session

- Work only inside this workspace and do not inspect parent or sibling paths.
- Do not restore an earlier session, access the network, use skills, delegate, or run Codex CLI.
- Follow only `WORK_ORDER.md` and the packaged schemas and validators.
- Treat each verifier task independently; do not compare it with other claims or verdicts.
- Do not seek calibration gold, human decisions, Layer A rationale, confidence, or hidden reasoning.
- Do not modify `sources/`, `input/`, `schemas/`, manifests, or instructions.
- Write only the required structured output under `output/`.
- Do not output hidden chain-of-thought, confidence percentages, or long rationale.

These are procedural context-isolation constraints, not an operating-system sandbox or a claim of statistical independence.
