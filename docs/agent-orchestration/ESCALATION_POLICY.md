# Escalation Policy

Stop and escalate to the Leader when the requested model is unavailable, ownership conflicts, a path is unsafe, evidence is missing, a reviewer asks to write, or scope needs a protected operation. Mark dynamic execution PARTIAL—not PASS—when model or resume capability is unavailable. Do not substitute a model or bypass a human checkpoint.
